[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: marks tests that require external services, network access, or real provider credentials
addopts =
    -q
    -m "not live"
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=80
