apiVersion: v1
kind: Service
metadata:
  name: medical-rag-chatbot
  namespace: medical-rag
spec:
  type: ClusterIP
  selector:
    app: medical-rag-chatbot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
